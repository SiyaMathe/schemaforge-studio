[sqlfluff]
dialect = tsql
templater = jinja
max_line_length = 120
# Ignore capitalization rules (CP) and layout/indentation rules (LT)
exclude_rules = capitalisation, layout

[sqlfluff:indentation]
tab_space_size = 4
indent_unit = space